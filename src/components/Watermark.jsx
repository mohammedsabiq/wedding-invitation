// Section background decoration. Kept minimal/modern — the clean gradient +
// subtle geometric texture (set on the section's pattern-* class) carries the
// look, so no extra floral ornament here.
export default function Watermark() {
  return null
}
